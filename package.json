{
  "name": "everyday-carry-11ty",
  "version": "1.0.0",
  "private": true,
  "description": "A small Eleventy site showing three everyday carry items (skeleton site — replace placeholders with your photos)",
  "scripts": {
    "start": "npx @11ty/eleventy --serve",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "@11ty/eleventy-img": "^6.0.4"
  },
  "dependencies": {
    "@11ty/eleventy-upgrade-help": "^3.0.1"
  }
}
